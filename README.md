Just a Go program that serves random memes from an images folder. For setup, just dump all the files in a folder, create a fodler called images inside said folder and put images in it.

Then run it. You can set a port in config.json or leave it empty for default (port 8080).
